package com.qvety.patients;

/** Mirrors the Postgres enum {@code species}. Labels come from i18n; adding one is a migration plus two translation lines. */
public enum Species {
    dog, cat, bird, rabbit, rodent, reptile, horse, livestock, other
}
