# Test profile. Datasource comes from Testcontainers via @ServiceConnection.
spring:
  flyway:
    locations: classpath:db/migration,classpath:db/seed

qvety:
  jwt:
    secret: test-only-secret-not-for-production-0123456789abcdef
    ttl: 12h

logging:
  level:
    com.qvety: DEBUG
